# Dify Plugin Marketplace 整改清单

更新时间：2026-08-26

状态说明：

- `[x]` 已完成并有检查依据
- `[ ]` 尚未完成
- `[?]` 需要重新验证或人工确认

## 1. 插件元数据

- [x] `manifest.yaml` 存在且 `type: plugin`
- [x] `manifest.yaml`、`pyproject.toml` 当前版本为 `0.1.0`
- [x] `author` 为 `modsdom`，不包含 `dify` 或 `langgenius`
- [x] `manifest.yaml` 注册了工具 Provider
- [x] `_assets/icon.svg` 和 `_assets/icon-dark.svg` 已存在
- [ ] 确认图标不是 Dify 默认模板图标
- [ ] 确认插件名称、英文描述和作者信息符合最终发布文案
- [ ] 确认 `minimum_dify_version` 是否需要设置为明确版本

## 2. 文档和隐私

- [x] 根目录 `README.md` 为英文
- [x] `PRIVACY.md` 存在且不是空模板
- [x] 完成 `readme/README_zh_Hans.md`，删除模板提示语和占位内容
- [x] 将 `readme/README_zh_Hans.md` 版本从 `0.0.1` 更新为 `0.1.0`
- [x] 在中文 README 中补充安装、配置、调用顺序和使用示例
- [x] 在英文 README 中补充完整的输入要求、SQL 限制和已知限制
- [ ] 复核隐私声明是否准确覆盖文件 URL、文件内容和临时文件处理

## 3. 运行时和依赖

- [x] `pyproject.toml` 与 `requirements.txt` 的依赖声明保持一致
- [x] 已声明 Python `>=3.12`
- [x] 已声明 `dify-plugin>=0.9.0,<0.10.0`
- [ ] 在可用的 Python 3.12 环境执行依赖安装
- [ ] 执行 `python -m compileall .`
- [ ] 执行 `python smoke_test.py`
- [ ] 在目标 Dify 实例中实际安装并启动插件
- [ ] 使用 Excel 和 CSV 文件分别验证两个工具

## 4. 打包内容

- [x] `dify plugin package .` 可以成功生成 `.difypkg`
- [x] 包内包含 `manifest.yaml`、`README.md`、`PRIVACY.md`、`_assets/`、Provider 和 Tools
- [x] 从 `.difyignore` 排除 `GUIDE.md` 和 `smoke_test.py` 等开发辅助文件
- [x] 清理并确认包内没有 `.env`、密钥、`.git`、虚拟环境、缓存或日志
- [x] 完成所有修改后重新生成最终 `excel_analysis.difypkg`
- [x] 解包检查最终包内容，并记录文件大小和校验值

## 5. GitHub 和 Marketplace 提交

- [x] 将源码提交到自己的公开 GitHub 仓库
- [x] Fork `langgenius/dify-plugins`
- [x] 在 `packages/excel_analysis/` 下只放一个 `excel_analysis.difypkg`
- [ ] 确认该版本尚未发布到 Marketplace
- [x] 使用英文填写 PR 标题和正文
- [x] 在 PR 中提供源码仓库、变更说明、测试环境、隐私安全和已知限制
- [x] 提交 PR（#2967）并通过当前已返回的自动检查
- [ ] 根据审核意见修复并重新推送
- [ ] PR 合并后确认插件出现在 `marketplace.dify.ai`

## 当前阻塞项

1. 当前环境无法正常启动 Python，依赖安装、编译和 smoke test 尚未完成。
2. 图标、最终发布文案和 `minimum_dify_version` 仍需人工确认。
3. 当前目录不是 Git 仓库，尚未完成源码仓库和官方 Marketplace PR 流程。

## 发布后维护

- [ ] 后续版本同步更新 `manifest.yaml`、`pyproject.toml` 和 README 版本
- [ ] 每次版本变更都重新打包，不复用旧 `.difypkg`
- [ ] 维护用户反馈、兼容性问题和隐私声明
